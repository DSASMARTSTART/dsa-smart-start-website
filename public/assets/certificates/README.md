# A1 Completion Certificate

Place the certificate file at:

```
a1-certificate.jpg
```

A single static JPEG (same image for every student — no per-user personalization).
The "Download Certificate" button on the A1 Final Test results screen serves this file directly with `download="A1-Certificate.jpg"`.
